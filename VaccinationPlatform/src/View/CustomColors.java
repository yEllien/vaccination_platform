package View;

import java.awt.Color;

public class CustomColors {
	static String ORANGE = "#FD7750";
	static String VERY_LIGHT_GRAY = "#F1F3F2";
	static String LIGHT_GRAY = "#DDDDDD";
	static String DARKER_LIGHT_GRAY = "#999999";
	static String GRAY = "#D9D9D9";
	static String LIGHTER_BLUE = "#BBD5FC";
	static String LIGHT_BLUE = "#84B1F4";
	static String DARK_BLUE = "#27418B";
	
	static Color orange = Color.decode(ORANGE);
	static Color very_light_gray = Color.decode(VERY_LIGHT_GRAY);
	static Color light_gray = Color.decode(LIGHT_GRAY);
	static Color darker_light_gray = Color.decode(DARKER_LIGHT_GRAY);
	static Color gray = Color.decode(GRAY);
	static Color lighter_blue = Color.decode(LIGHTER_BLUE);
	static Color light_blue = Color.decode(LIGHT_BLUE);
	static Color dark_blue = Color.decode(DARK_BLUE);
}
